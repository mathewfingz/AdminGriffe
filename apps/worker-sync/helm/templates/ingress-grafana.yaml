{{- if and .Values.monitoring.grafana.enabled .Values.monitoring.grafana.ingress.enabled -}}
{{- $fullName := include "worker-sync.fullname" . -}}
{{- $svcPort := .Values.monitoring.grafana.service.port -}}
{{- if and .Values.monitoring.grafana.ingress.className (not (hasKey .Values.monitoring.grafana.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.monitoring.grafana.ingress.annotations "kubernetes.io/ingress.class" .Values.monitoring.grafana.ingress.className}}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-grafana
  labels:
    {{- include "worker-sync.labels" . | nindent 4 }}
    app.kubernetes.io/component: grafana
  {{- with .Values.monitoring.grafana.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.monitoring.grafana.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.monitoring.grafana.ingress.className }}
  {{- end }}
  {{- if .Values.monitoring.grafana.ingress.tls }}
  tls:
    {{- range .Values.monitoring.grafana.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.monitoring.grafana.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-grafana
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-grafana
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}